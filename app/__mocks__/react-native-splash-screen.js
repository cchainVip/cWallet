export default {
	show: jest.fn().mockImplementation(() => {
		// eslint-disable-next-line no-console
	}),
	hide: jest.fn().mockImplementation(() => {
		// eslint-disable-next-line no-console
	}),
};
